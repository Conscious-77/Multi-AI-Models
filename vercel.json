
{
  "version": 2,
  "builds": [{ "src": "package.json", "use": "@vercel/static-build" }],
  "routes": [
    { "src": "/api/(.*)", "dest": "https://multi-ai-models-production.up.railway.app/api/$1" }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
